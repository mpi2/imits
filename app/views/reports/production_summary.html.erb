
<%= form_tag( { :controller => 'reports', :action => 'production_summary' }, :method => :get ) do |form| %>
  <%= render 'report_controls' %>
  <%= submit_tag 'Go' %>
<% end %>

<% unless params[:commit].blank? %>
  <div id="report">
    <%=raw @production_summary.to_html %>
  </div>
<% end %>
