<tr>
    <td>
        <%= f.text_field :name %>
    </td>
    <td>
        <a>QC Data (click to edit)</a>
    </td>
    <td>
        <a>Upload file</a>
    </td>
    <td>
        <a>Allele</a>
    </td>
    <td>
        <%= f.check_box :genotype_confirmed %>
    </td>
    <td>
        <% if f.object.new_record? %>
            <%= f.hidden_field :_destroy, :class => 'destroy-field' %>
            <%= link_to "remove", '#', class: "remove-row" %>
        <% else %>
            <%= f.hidden_field :_destroy, :class => 'destroy-field' %>
            <%= link_to "remove", '#', class: "hide-row" %>
        <% end %>
    </td>
</tr>
