<% @title = 'Production Summary (feed)' %>

<h2><%= @title2 %></h2>

<% if @csv %>

<p><%= link_to('Download as CSV', url_for({:action => :summary_by_consortium_and_accumulated_status, :controller => '/reports/mi_production', :format => :csv}.merge(params))) if @csv %></p>

<br/>
<% end %>

<div class="report grid_12">

<%= raw @report.to_html if @report %>

</div>
