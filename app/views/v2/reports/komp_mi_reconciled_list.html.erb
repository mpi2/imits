<% @title = 'Mi Attempt Distribution centres Komp Repository Reconciled Report' %>
<h3># KOMP Reconciled Mi Attempt Distribution Centres</h3>
<h4>Consortium: <%= @consortium %> and Production Centre: <%= @prod_centre %></h4>
<p>These are the Genotype Confirmed clones that are available at Komp</p>
<p><%= link_to 'Download as CSV', report_csv_path %></p>
<div class='report'>

    <table>
      <thead>
        <tr>
          <th>Consortium</th>
          <th>Production Centre</th>
          <th>Gene Marker Symbol</th>
          <th>Mi Attempt Colony Name</th>
          <th>Allele Type</th>
          <th>Reconciled Date</th>
        </tr>
      </thead>
      <tbody>
        <% @komp_reconciled_list.each do |report_row| %>
            <tr>
                <td><%= @consortium %></td>
                <td><%= @prod_centre %></td>
                <td><%= report_row['marker_symbol'] %></td>
                <td><%= report_row['mi_attempt_colony_name'] %></td>
                <% if report_row['mouse_allele_type'].blank? %>
                  <td><%= report_row['allele_type'] %></td>
                <% else %>
                  <td><%= report_row['mouse_allele_type'] %></td>
                <% end %>
                <td><%= report_row['reconciled_date'] %></td>
            </tr>
        <% end %>

      </tbody>
    </table>

</div>