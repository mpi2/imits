<% @title = 'Production Summaries' %>

<h2><%= @title2 %></h2>

<p><%= link_to('Download as CSV', url_for({:action => controller.action_name, :controller => '/reports/mi_production', :format => :csv}.merge(params))) if @csv %></p>

<% if @csv %><br/><% end %>

<div class="report grid_12">

<%= raw @report.to_html if @report %>

</div>
