<% content_for :pre_js_footer do %>
  <%= javascript_tag "CONSORTIUM_OPTIONS = #{Consortium.all.map(&:name).sort.to_json}" %>
  <%= javascript_tag "CENTRE_OPTIONS = #{Centre.all.map(&:name).sort.to_json}" %>
  <%= javascript_tag "USER_PRODUCTION_CENTRE = #{current_user ? current_user.production_centre.name.to_json : ''}" %>
  <%= javascript_tag "SUB_PROJECT_OPTIONS = #{SubProject.all.map(&:name).sort.to_json}" %>
  <%= javascript_tag "CAN_SEE_SUB_PROJECT = #{current_user ? current_user.can_see_sub_project?.to_json: false}" %>
  <%= javascript_tag "PRIORITY_OPTIONS = #{PlanIntention::AlleleIntention::Priority.all.map(&:name).to_json}" %>
<% end %>
